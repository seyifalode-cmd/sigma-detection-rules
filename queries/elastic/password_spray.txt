event.code: "4625" and winlog.channel: "Security"
# Threshold rule: group by source.ip
# Alert when dcount(winlog.event_data.TargetUserName) > 20 within a 10 minute window
# Elastic SIEM Threshold Rule configuration:
#   Field: source.ip
#   Threshold field: winlog.event_data.TargetUserName (unique count)
#   Threshold value: 20
#   Time window: 10m
